# PDF Scanner Chrome Extension - Environment Configuration
# Copy this file to .env and fill in your values

# Development settings
DEV_BACKEND_URL=http://localhost:3001
DEV_BACKEND_PORT=3001

# Production settings (fill in for deployment)
PROD_BACKEND_URL=https://api.your-backend.com
PROD_BACKEND_PORT=443

# Prompt Security API Configuration (REQUIRED)
# Sign up at https://www.prompt.security/ to get these credentials
# Extension requires API for secret detection - gracefully allows uploads if unavailable
PROMPT_SECURITY_APP_ID=your-app-id-here

# Optional: API Key if required by your Prompt Security plan
# PROMPT_SECURITY_API_KEY=your-api-key-here 