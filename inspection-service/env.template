# PDF Scanner Inspection Service - Environment Configuration
# Copy this file to .env and fill in your values

# Server Configuration
NODE_ENV=development
INSPECTION_PORT=3001

# Prompt Security API Configuration (REQUIRED)
# Sign up at https://www.prompt.security/ to get these credentials
# Service will gracefully allow uploads if API is unavailable
PROMPT_SECURITY_APP_ID=
PROMPT_SECURITY_API_KEY=

# Development Settings
LOG_LEVEL=debug

# Security Settings (Optional)
# Comma-separated list of allowed origins for CORS
# ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Performance Settings (Optional)
# Maximum file size for PDF uploads (default: 20MB)
# MAX_FILE_SIZE=20971520
# PDF parsing cache size limit (default: 100)
# CACHE_SIZE_LIMIT=100 